<section class="section section--text">
  <div class="section__inner">
    {% if block.title %}
    <h2 class="section__title">{{ block.title }}</h2>
    {% endif %}
    <div class="section__body">{{ block.body }}</div>
  </div>
</section>
